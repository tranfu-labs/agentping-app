# Ageng网络医生（AgentPing）产品方案

## 1. 产品一句话

Ageng网络医生是给 Codex、Claude Code、Cursor 等 AI Agent 使用者准备的网络状态监测与诊断工具。AgentPing 保留为内部工具名和命令行名称。

当 Agent 一直没反应时，它不再只让用户猜“是不是网络又抽了”，而是直接告诉用户：现在是哪里卡住了、严重不严重、下一步该怎么处理。

## 2. 首版核心定位

首版只解决一件事：

**把 Agent 网络卡住这件事，从黑盒变成可见、可解释、可行动。**

不要一开始就做成完整网络接管工具。原因是网络接管需要更高信任，用户首先需要相信 AgentPing 的判断是准的。

## 3. 目标用户

第一批用户不是所有开发者，而是这类人：

- 在中国大陆使用 Codex、Claude Code、Cursor、Claude Desktop 等 AI 工具。
- 经常遇到 Agent 运行中突然没反应。
- 使用 VPN、代理、机场、公司网络或自建节点。
- 不确定问题来自本地代理、出口 IP、官方服务，还是 AI 工具本身。
- 现在的处理方式主要是反复重启、换节点、换软件、等一会儿。

## 4. 用户痛点

用户真正痛的不是“网络慢”，而是：

- 卡住时不知道发生了什么。
- 不知道该重启 Agent、换节点、改代理规则，还是等官方恢复。
- 每次排查都靠经验和玄学。
- Agent 执行到一半失败，会浪费上下文、时间和心情。
- 代理工具能显示节点在线，但不能说明 Agent 到 API 的真实链路是否可用。

## 5. 首版产品形态

首版建议做两个入口：

### 5.1 终端命令

适合开发者快速检查：

```text
agentping check
```

输出效果应该是人能直接看懂的：

```text
Claude API：异常
原因：出口 IP 可能被拦截
建议：切换节点，优先选择非共享出口
延迟：无法建立连接
```

### 5.2 菜单栏小挂件

适合长期观察：

```text
绿 180ms
黄 850ms
红 blocked
```

菜单点开后显示：

- Codex 链路状态
- Claude 链路状态
- OpenAI 链路状态
- Anthropic 链路状态
- 最近一次异常原因
- 建议动作

## 6. 首版必须做的能力

### 6.1 链路健康灯

用红、黄、绿表示当前健康度：

- 绿：可用，延迟正常。
- 黄：可用，但延迟高或不稳定。
- 红：不可用，或者失败率明显异常。

### 6.2 一键诊断

用户执行一次诊断后，得到明确结果：

- 当前 Agent 相关接口是否可连。
- DNS 是否慢或异常。
- TCP 是否能连上。
- 是否出现超时。
- 是否疑似被出口 IP 拦截。
- 是否可能是官方 API 服务异常。
- 是否可能是本地代理规则没有命中。

### 6.3 根因归类

首版至少归成 6 类：

1. 本地网络不可用。
2. 本地代理未生效。
3. 代理分流规则异常。
4. 出口节点质量差或被拦截。
5. 目标 API 服务异常。
6. Agent 工具本身无响应，但网络链路正常。

### 6.4 诊断报告

每次诊断后生成一份简短报告，方便用户自己看，也方便发给别人协助排查。

报告不要写成工程日志，要写成人话：

```text
本次检查显示：你的本地网络正常，代理也在工作，但访问 Anthropic API 时连接被拒绝。
更可能的问题是当前出口节点不适合访问 Claude。
建议先切换节点，再重新运行 Claude Code。
```

## 7. 首版先不做什么

首版不要做这些：

- 不接管全部系统网络。
- 不内置代理服务。
- 不自动切换用户节点。
- 不保存用户 API Key。
- 不做复杂团队后台。
- 不做完整流量分析。
- 不承诺解决所有网络问题。

首版要克制，重点是“判断准”和“说清楚”。

## 8. 推荐信息展示

### 8.1 状态栏

```text
AgentPing  绿  162ms
```

异常时：

```text
AgentPing  红  Claude blocked
```

### 8.2 终端输出

```text
AgentPing 诊断结果

Codex/OpenAI：正常，延迟 186ms
Claude/Anthropic：异常，连接失败
本地代理：正常
DNS：正常

判断：当前出口节点可能无法访问 Anthropic。
建议：切换节点后重试；如果多个节点都失败，再检查官方状态。
```

### 8.3 异常提醒

```text
Claude Code 可能不是卡死，而是 Anthropic 链路不可用。
```

## 9. 第一版使用流程

### 场景：Claude Code 一直没反应

1. 用户发现 Claude Code 执行任务一直没有输出。
2. 用户点击菜单栏 AgentPing，看到 Anthropic 为红色。
3. AgentPing 显示：本地代理正常，但 Anthropic 连接失败。
4. 用户切换节点。
5. AgentPing 变成绿色。
6. 用户重新运行 Claude Code。

这个流程能成立，第一版就有价值。

## 10. 判断逻辑的产品表达

产品不要对用户说“技术检测项 A/B/C”。

产品要对用户说：

- “你的电脑能上网。”
- “代理没有接管这条请求。”
- “当前节点可以访问 OpenAI，但访问 Claude 失败。”
- “更像是节点问题，不像是官方服务挂了。”
- “网络是通的，可能是 Agent 软件本身卡住了。”

## 11. 第一版成功标准

第一版做完后，用这几个标准判断是否成功：

- 用户能在 10 秒内知道现在 Agent 网络健康不健康。
- 用户遇到卡住时，不需要先重启软件，而是先看诊断。
- 诊断结果至少能把问题分到一个可信类别。
- 用户看完建议后知道下一步该做什么。
- 用户愿意把报告发给别人，因为它比自己描述问题更清楚。

## 12. 产品路线

### V0：本地诊断

目标：先证明判断有用。

能力：

- 终端检查命令。
- 菜单栏状态灯。
- OpenAI / Anthropic 链路检测。
- 本地代理状态识别。
- 简单诊断报告。

### V1：持续监控

目标：让用户在 Agent 运行前就知道网络是否适合开工。

能力：

- 后台定时检测。
- 异常历史记录。
- 节点稳定性评分。
- 常见问题提示。
- 一键复制诊断报告。

### V2：辅助修复

目标：从“告诉你问题”进化到“帮你处理问题”。

能力：

- 推荐可用节点。
- 失败后自动重试。
- 根据不同 Agent 选择不同访问策略。
- 对常见代理客户端给出配置建议。

### V3：Agent 专属网关

目标：从底层减少网络不稳定。

能力：

- Agent 请求专属通道。
- 智能节点漂移。
- API 请求重试。
- 团队级网络质量看板。
- 内容和教程体系。

## 13. 商业化方向

首版可以先做个人工具，再扩展团队。

### 免费版

- 手动检查。
- 基础状态灯。
- 最近一次诊断结果。

### 个人专业版

- 持续监控。
- 异常历史。
- 节点评分。
- 诊断报告。
- 多 Agent 支持。

### 团队版

- 团队网络质量统计。
- 团队常见异常聚合。
- 统一网关。
- 成员使用体验监控。

## 14. 内容选题

AgentPing 的内容不要泛泛讲网络知识，要围绕真实故障写。

优先写这些：

- Codex 一直没反应，怎么判断是不是网络问题？
- Claude Code 卡住时，最常见的 6 个原因。
- 为什么浏览器能访问 Claude，但 Claude Code 不能用？
- OpenAI 正常，Anthropic 异常，通常是什么原因？
- VPN 节点在线，不代表 Agent API 链路可用。
- 中国大陆使用 AI Agent，如何做网络自检？

内容目标不是单纯科普，而是把用户引导到：

```text
下次卡住，先跑 AgentPing。
```

## 15. 首个可交付版本

第一版建议交付：

1. 一个命令行工具。
2. 一个菜单栏状态灯。
3. 一个诊断报告模板。
4. 一篇说明文章。

第一篇文章题目建议：

```text
Claude Code 一直没反应？先别重启，先判断是不是网络链路坏了
```

## 16. 关键风险

### 16.1 判断不准

如果诊断经常误判，用户会很快失去信任。

应对方式：

- 不要强行下结论。
- 用“更可能是”表达概率。
- 提供证据和建议动作。

### 16.2 首版做太大

如果一开始就做网络接管，会变成复杂代理工具。

应对方式：

- 先做可观察。
- 再做可解释。
- 最后做可接管。

### 16.3 用户担心隐私

网络工具容易让用户担心流量和密钥。

应对方式：

- 首版不读取用户 API Key。
- 不上传请求内容。
- 只检测连接状态。
- 报告里不包含敏感信息。

## 17. 现在最该做的下一步

不要马上做完整 App。

下一步应该先做一个最小可用原型：

```text
agentping check
```

它只要能回答 3 个问题：

1. OpenAI 链路现在通不通？
2. Anthropic 链路现在通不通？
3. 如果不通，更像是哪一类问题？

这个原型跑通后，再做菜单栏挂件。
